/* eslint-disable react/prop-types */
import React from "react";

const HeadingQuinary = (props) => {
  const { heading } = props;
  return <h5 className="heading-quinary u-mb-sm">{heading}</h5>;
};

export default HeadingQuinary;
