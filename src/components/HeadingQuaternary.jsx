/* eslint-disable react/prop-types */
import React from "react";

const HeadingQuaternary = (props) => {
  const { heading } = props;

  return <h4 className="heading-quaternary u-mb-t">{heading}</h4>;
};

export default HeadingQuaternary;
